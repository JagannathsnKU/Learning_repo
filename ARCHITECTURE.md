# DreamScape Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     DREAMSCAPE APPLICATION                      │
│                     (React 18 + TypeScript)                     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      PRESENTATION LAYER                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    App.tsx (Main)                          │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │              Phase Management                        │  │ │
│  │  │  • Idle → Recording → Interpreting → Viewing         │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │         Component Tree (4 Phase Components)          │  │ │
│  │  │  • PhaseIdle    - Welcome screen                     │  │ │
│  │  │  • PhaseRecording - Voice input                      │  │ │
│  │  │  • PhaseInterpreting - Loading state                 │  │ │
│  │  │  • PhaseViewing - Dream visualization                │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    COMPONENT LIBRARY                       │ │
│  │  ┌───────────────────┬───────────────────────────────────┐ │ │
│  │  │ GlassUI.tsx       │ VoiceRecorder.tsx                 │ │ │
│  │  │ ┌───────────────┐ │ ┌─────────────────────────────┐   │ │ │
│  │  │ │ GlassPanel    │ │ │ • Start Recording           │   │ │ │
│  │  │ │ GlassButton   │ │ │ • Live Transcription        │   │ │ │
│  │  │ │ GlassCard     │ │ │ • Manual Input              │   │ │ │
│  │  │ │ GlassInput    │ │ │ • Error Handling            │   │ │ │
│  │  │ │ GlassBadge    │ │ │ • Submit Dream              │   │ │ │
│  │  │ │ LoadingSpinner│ │ └─────────────────────────────┘   │ │ │
│  │  │ └───────────────┘ │                                   │ │ │
│  │  └───────────────────┴───────────────────────────────────┘ │ │
│  │  ┌───────────────────────┬───────────────────────────────┐ │ │
│  │  │ Dream3DRenderer.tsx   │ Dream2DRenderer.tsx           │ │ │
│  │  │ ┌───────────────────┐ │ ┌───────────────────────────┐ │ │ │
│  │  │ │ Three.js Canvas   │ │ │ HTML5 Canvas              │ │ │ │
│  │  │ │ • Geometry Setup  │ │ │ • Shape Rendering         │ │ │ │
│  │  │ │ • Lighting        │ │ │ • Glow Effects            │ │ │ │
│  │  │ │ • Animation Loop  │ │ │ • Grid Background         │ │ │ │
│  │  │ │ • Mouse Control   │ │ │ • Mouse Interaction       │ │ │ │
│  │  │ └───────────────────┘ │ └───────────────────────────┘ │ │ │
│  │  └───────────────────────┴───────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     STATE MANAGEMENT LAYER                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              Zustand Store (appStore.ts)                   │ │
│  │  ┌─────────────────────────────────────────────────────┐   │ │
│  │  │ State:                                              │   │ │
│  │  │ • currentDreamMap: DreamMap                         │   │ │
│  │  │ • currentSceneIndex: number                         │   │ │
│  │  │ • renderMode: '2d' | '3d'                           │   │ │
│  │  │ • recorderState: RecorderState                      │   │ │
│  │  │ • interpreterState: InterpreterState                │   │ │
│  │  │ • isExploring: boolean                              │   │ │
│  │  │ • shareToken: string | null                         │   │ │
│  │  └─────────────────────────────────────────────────────┘   │ │
│  │  ┌─────────────────────────────────────────────────────┐   │ │
│  │  │ Actions:                                            │   │ │
│  │  │ • setCurrentDreamMap()                              │   │ │
│  │  │ • setRenderMode()                                   │   │ │
│  │  │ • setRecorderState()                                │   │ │
│  │  │ • setInterpreterState()                             │   │ │
│  │  │ • resetApp()                                        │   │ │
│  │  └─────────────────────────────────────────────────────┘   │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      SERVICES LAYER                             │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │             dreamInterpreter.ts (AI Service)               │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │ Input: narration (string)                            │  │ │
│  │  │ ├─ extractMood()                                     │  │ │
│  │  │ ├─ extractColors()                                   │  │ │
│  │  │ ├─ generateDreamElements()                           │  │ │
│  │  │ ├─ generateLightingSetup()                           │  │ │
│  │  │ └─ generateFogSetup()                                │  │ │
│  │  │ Output: DreamMap (JSON)                              │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              speechToText.ts (Web Speech API)               │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │ • SpeechToTextService class                          │  │ │
│  │  │ • Browser microphone access                          │  │ │
│  │  │ • Real-time transcription callbacks                  │  │ │
│  │  │ • Error handling & fallbacks                         │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       TYPES & INTERFACES                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ DreamMap                                                   │ │
│  │ ├─ id, title, narration                                   │ │
│  │ ├─ scenes: DreamScene[]                                   │ │
│  │ └─ shareToken?, isPublic                                  │ │
│  │                                                            │ │
│  │ DreamScene                                                │ │
│  │ ├─ id, title, narration, timestamp                        │ │
│  │ ├─ mood, colors                                           │ │
│  │ ├─ elements: DreamElement[]                               │ │
│  │ ├─ lighting: LightingSetup                                │ │
│  │ └─ fog: FogSetup                                          │ │
│  │                                                            │ │
│  │ DreamElement                                              │ │
│  │ ├─ id, type, name, description                            │ │
│  │ ├─ position: Vector3D                                     │ │
│  │ ├─ scale, color, mood                                     │ │
│  │ └─ surreal                                                │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    STYLING LAYER (Tailwind CSS)                 │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Glassmorphism Design System                               │ │
│  │ ├─ Colors: Blue, Purple, Dark palette                     │ │
│  │ ├─ Backdrop Filters: 10-20px blur                         │ │
│  │ ├─ Opacity: 8-15% white for glass effect                  │ │
│  │ ├─ Shadows: Glow effects for depth                        │ │
│  │ ├─ Animations: Fade, scale, float                         │ │
│  │ └─ Typography: Minimalist, system fonts                   │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Architecture

```
┌─────────────────────────┐
│    User Input Layer     │
│                         │
│  Voice Recording or     │
│  Text Input             │
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│   Speech Recognition Processing     │
│                                     │
│  Web Speech API                     │
│  ├─ Microphone access               │
│  ├─ Real-time transcription         │
│  └─ Live display                    │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│    Transcription Output             │
│                                     │
│  String: "I was flying through..."  │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│   AI Dream Interpretation           │
│                                     │
│  • Mood extraction                  │
│  • Color extraction                 │
│  • Element generation               │
│  • Spatial mapping                  │
│  • Lighting setup                   │
│  • Fog configuration                │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│    Structured Dream Map             │
│                                     │
│  DreamMap {                         │
│    id, title, narration             │
│    scenes: [{                       │
│      elements: [...]                │
│      mood, colors                   │
│      lighting, fog                  │
│    }]                               │
│  }                                  │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│    State Update (Zustand)           │
│                                     │
│  setCurrentDreamMap(dreamMap)       │
│  setInterpreterState(success)       │
└────────────┬────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────┐
│   Visualization Rendering                    │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │        3D Renderer (Three.js)          │  │
│  │ ┌──────────────────────────────────┐   │  │
│  │ │ • Create geometry per element    │   │  │
│  │ │ • Apply materials & colors       │   │  │
│  │ │ • Set up lighting system         │   │  │
│  │ │ • Configure camera & controls    │   │  │
│  │ │ • Start animation loop           │   │  │
│  │ └──────────────────────────────────┘   │  │
│  └────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────┐  │
│  │      2D Renderer (Canvas)              │  │
│  │ ┌──────────────────────────────────┐   │  │
│  │ │ • Draw shapes per element        │   │  │
│  │ │ • Apply colors & gradients       │   │  │
│  │ │ • Add glow effects               │   │  │
│  │ │ • Enable mouse interactions      │   │  │
│  │ │ • Animate continuously           │   │  │
│  │ └──────────────────────────────────┘   │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────┐
│   Interactive Exploration                    │
│                                              │
│  • Mouse camera control                      │
│  • Element hover/selection                   │
│  • Animation loops                           │
│  • View mode switching                       │
└──────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────┐
│   Sharing & Export                           │
│                                              │
│  ├─ Generate share token                     │
│  ├─ Copy share URL to clipboard              │
│  ├─ Export dream to JSON file                │
│  └─ Create read-only viewer link             │
└──────────────────────────────────────────────┘
```

---

## 📁 File Dependency Graph

```
App.tsx (Main)
├── GlassUI.tsx (Components)
│   ├── tailwind.config.js (Styles)
│   └── index.css (Global CSS)
├── VoiceRecorder.tsx
│   ├── GlassUI.tsx
│   └── speechToText.ts
│       └── Web Speech API
├── Dream3DRenderer.tsx
│   ├── Three.js library
│   └── types/index.ts (DreamScene)
├── Dream2DRenderer.tsx
│   ├── HTML5 Canvas API
│   └── types/index.ts (DreamScene)
└── appStore.ts (State)
    ├── Zustand library
    ├── types/index.ts
    └── dreamInterpreter.ts
        ├── Mock AI service
        └── types/index.ts

main.tsx (Entry Point)
└── App.tsx

index.html
├── main.tsx
└── Tailwind CSS

Configuration Files
├── vite.config.ts
├── tsconfig.json
├── tailwind.config.js
├── postcss.config.js
└── package.json
```

---

## 🎯 Component Lifecycle

```
MOUNT
  │
  ├─ App renders
  │  └─ Phase: 'idle'
  │
  ├─ PhaseIdle component displays
  │  └─ Features cards rendered
  │
  ├─ User clicks "Start Recording"
  │  └─ Phase changes to 'recording'
  │
  ├─ VoiceRecorder component mounts
  │  ├─ Web Speech API initialized
  │  └─ Microphone permission requested
  │
  ├─ User speaks or types
  │  ├─ Transcription updated in real-time
  │  └─ Display refreshes
  │
  ├─ User clicks "Interpret Dream"
  │  ├─ Phase changes to 'interpreting'
  │  ├─ dreamInterpreter.interpretDream() called
  │  └─ Zustand store state updates
  │
  ├─ AI processing complete (1.5s)
  │  ├─ DreamMap generated
  │  ├─ Phase changes to 'viewing'
  │  └─ State updates via setCurrentDreamMap()
  │
  ├─ PhaseViewing component renders
  │  ├─ Dream3DRenderer OR Dream2DRenderer mounts
  │  ├─ Animation loop starts
  │  └─ Interactive controls activated
  │
  ├─ User explores dream
  │  ├─ Mouse moves → Camera rotates
  │  ├─ Animation loop continues
  │  └─ No component re-renders
  │
  ├─ User clicks "Share" or "Export"
  │  ├─ generateShareToken() or exportDreamAsFile() called
  │  └─ Action completed
  │
  └─ User clicks "Back to Start"
     ├─ resetApp() called
     ├─ All state cleared
     ├─ Animation loops cancelled
     └─ Phase changes to 'idle'

UNMOUNT
```

---

## 🔌 API Integration Points

```
┌──────────────────────────────────────┐
│  Web Browser APIs                    │
├──────────────────────────────────────┤
│                                      │
│  Web Speech API                      │
│  ├─ webkitSpeechRecognition          │
│  ├─ Microphone input                 │
│  └─ Real-time transcription          │
│                                      │
│  Canvas API                          │
│  ├─ 2D rendering context             │
│  ├─ Shape drawing                    │
│  └─ Animation loops                  │
│                                      │
│  WebGL / Three.js                    │
│  ├─ 3D rendering context             │
│  ├─ Geometry, lighting, cameras      │
│  └─ GPU acceleration                 │
│                                      │
│  Clipboard API                       │
│  ├─ Copy share URLs                  │
│  └─ Browser notification             │
│                                      │
│  DOM APIs                            │
│  ├─ Event listeners                  │
│  ├─ DOM manipulation                 │
│  └─ Ref management                   │
│                                      │
│  Window APIs                         │
│  ├─ Window resize                    │
│  ├─ Request animation frame          │
│  └─ Local environment info           │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│  Ready for External APIs             │
├──────────────────────────────────────┤
│                                      │
│  LLM Services (Future)               │
│  ├─ OpenAI API                       │
│  ├─ Anthropic API                    │
│  └─ Llama API                        │
│                                      │
│  Database (Future)                   │
│  ├─ MongoDB                          │
│  ├─ PostgreSQL                       │
│  └─ Firebase                         │
│                                      │
│  Authentication (Future)             │
│  ├─ Auth0                            │
│  ├─ Firebase Auth                    │
│  └─ Custom JWT                       │
│                                      │
│  Analytics (Future)                  │
│  ├─ Google Analytics                 │
│  ├─ Sentry                           │
│  └─ Custom logging                   │
└──────────────────────────────────────┘
```

---

## 🎨 Style System Architecture

```
Global Styles (index.css)
├─ Tailwind directives (@tailwind)
├─ Reset styles (*, html, body)
├─ Custom keyframes (@keyframes)
├─ Utility classes (.glass, .glow)
├─ Scrollbar styling (::-webkit-scrollbar)
└─ Selection color (::selection)

Tailwind Config (tailwind.config.js)
├─ Content paths for purging
├─ Theme extensions
│  ├─ Colors (glass-dark, dream-blue, etc.)
│  ├─ Animations (fade-in, scale-in, float)
│  ├─ Box shadows (glow effects)
│  └─ Fonts (system font stack)
├─ Backdrop filters (blur effects)
└─ Plugins (PostCSS)

Component Styles
├─ Tailwind utility classes
├─ Responsive prefixes (md:, lg:)
├─ State variants (hover:, focus:, disabled:)
└─ Animations (animate-fade-in, animate-float)

PostCSS Processing (postcss.config.js)
├─ Tailwind CSS plugin
├─ Autoprefixer for compatibility
└─ Output: Optimized CSS
```

---

## 🚀 Build & Deployment Pipeline

```
Source Code
│
├─ TypeScript compilation
│  └─ tsconfig.json rules
│
├─ Vite bundling
│  ├─ React plugin
│  └─ Code splitting
│
├─ Tailwind CSS processing
│  ├─ PostCSS
│  ├─ Autoprefixer
│  └─ PurgeCSS
│
├─ Asset optimization
│  ├─ Minification
│  ├─ Compression
│  └─ Source maps
│
└─ dist/ folder
   ├─ index.html
   ├─ bundle.js
   ├─ bundle.css
   └─ assets/
      └─ (images, fonts, etc.)

Deployment Options
├─ Vercel (recommended)
├─ Netlify
├─ GitHub Pages
├─ AWS S3 + CloudFront
└─ Docker container
```

---

This architecture provides a **scalable, maintainable, and extensible** foundation for DreamScape's current and future development.

---

**DreamScape: Where imagination meets technology.** ✨
